spring:
  datasource:
    url: jdbc:mysql://localhost:3306/labsdb?useSSL=false
    username: root
    password: DaiMneSil1
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
